// Plugins de processamento de CSS
// O Tailwind e o Autoprefixer precisam ser registrados aqui
export default {
  plugins: {
    tailwindcss: {},   // Gera as classes utilitárias do Tailwind
    autoprefixer: {},  // Adiciona prefixos de browser automaticamente (ex: -webkit-)
  },
}
